{
  "name": "test",
  "version": "1.0.0",
  "dependencies": {
    "ejs": "^3.1.6",
    "mongodb": "*",
    "nodemon": "^2.0.7"
  },
  "description": "",
  "main": "app.js",
  "bin": {
    "test": "www.js"
  },
  "scripts": {
    "start": "npm run kill && nodemon ./bin/www.js",
    "kill": "kill -9 $(lsof -i :3000) &>/dev/null | exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
